console.log('db:seed executado.');
console.log('O catálogo desta branch já é gerado em runtime por src/lib/catalog.ts.');
console.log('Se quiser persistência real no Supabase, primeiro crie as tabelas usando npm run db:generate e depois integre seeds SQL reais.');
