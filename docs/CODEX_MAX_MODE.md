# Codex Max Mode

Generated for the industrial platform work.

- Requested model: gpt-5.5.
- Active session limitation: the running Codex desktop session cannot switch model from inside the assistant turn.
- Suggested CLI command when available: `codex -m gpt-5.5`.
- Environment: local Windows PowerShell in `M:\LOJA\mdh-prod-deploy`.
- Branch at baseline: `codex/visual-fase14-cinematic`.
- Sandbox: filesystem unrestricted, network enabled, approval policy never.
- Goal Mode: active.
- Secrets policy: no `.env`, tokens or real secrets are printed or committed.
- Baseline evidence: `reports/industrial/baseline/`.
